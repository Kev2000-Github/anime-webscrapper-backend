# anime-webscrapper-backend
This is a webscrapper that gets the anime list of `anime-planet.com` and stores it 
in a mongoDB database once a month

Install dependencies

`npm install`

Run development mode

`npm run dev`
